<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ site.title }}</title>
    {% include "head.njk" %}
  </head>
  <body>
    <div class="max-content spacing--inset">{{ content | safe }}</div>
  </body>
</html>