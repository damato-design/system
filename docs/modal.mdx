import { Meta } from '@storybook/blocks';

<Meta title="Components/Modal" />

# Modal

Don't use a modal. There's often a better pattern.

### Double confirmation

Instead of launching a modal to double confirm a user's critical action, consider causing the user to type something in a field.